'use client';
import {useState} from 'react';
import {ArrowLeft,ArrowRight,LockKeyhole} from 'lucide-react';
export default function AdminLogin(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setMsg('');const r=await fetch('/api/admin-login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json().catch(()=>({}));if(!r.ok)return setMsg(d.error||'Admin sign-in failed.');window.location.href='/admin'};return <main className="auth-shell"><a className="auth-back" href="/"><ArrowLeft size={15}/> Back to store</a><section className="auth-card"><div className="auth-art"><span className="auth-mini"><LockKeyhole size={14}/> ADMIN ACCESS</span><h1>Private control for your store.</h1><p>Admin access is separated from the customer account and protected by a server-side Vercel session.</p></div><div className="auth-panel"><span className="eyebrow">LUMA ADMIN</span><h2>Admin login</h2><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label><button className="primary-btn full" type="submit">Continue to admin <ArrowRight size={16}/></button></form>{msg&&<div className="auth-message">{msg}</div>}<small className="auth-legal">Set LUMA_ADMIN_EMAIL and LUMA_ADMIN_PASSWORD in Vercel. Never enter bKash/Nagad PINs or OTPs here.</small></div></section></main>}