'use client';
import {useState} from 'react';
import {ArrowLeft,ArrowRight,ShieldCheck} from 'lucide-react';
export default function Login(){const[m,setM]=useState('');const go=()=>{const email=prompt('Enter your email');if(!email)return;const name=prompt('Enter your name')||email.split('@')[0];localStorage.setItem('luma-user',JSON.stringify({name,email}));window.location.href='/account'};return <main className="auth-shell"><a className="auth-back" href="/"><ArrowLeft size={15}/> Back to store</a><section className="auth-card"><div className="auth-art"><span className="auth-mini"><ShieldCheck size={14}/> LUMA ACCOUNT</span><h1>Your shopping, connected.</h1><p>Keep your local order history, favorites, and checkout profile together.</p></div><div className="auth-panel"><span className="eyebrow">WELCOME BACK</span><h2>Sign in</h2><p>Use the simple LUMA customer account on this free deployment.</p><button className="primary-btn full" onClick={go}>Continue <ArrowRight size={16}/></button><button className="secondary-btn full" onClick={()=>window.location.href='/signup'}>Create a new account</button>{m&&<div className="auth-message">{m}</div>}<small className="auth-legal">Customer profile is stored on this device. No provider passwords are collected.</small></div></section></main>}