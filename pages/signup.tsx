'use client';
import {ArrowLeft,ArrowRight,UserPlus} from 'lucide-react';
export default function Signup(){const go=()=>{const name=prompt('Your name');if(!name)return;const email=prompt('Your email');if(!email)return;localStorage.setItem('luma-user',JSON.stringify({name,email}));window.location.href='/account'};return <main className="auth-shell"><a className="auth-back" href="/"><ArrowLeft size={15}/> Back to store</a><section className="auth-card"><div className="auth-art"><span className="auth-mini"><UserPlus size={14}/> NEW CUSTOMER</span><h1>Make checkout feel effortless.</h1><p>Create a free local LUMA profile and keep your purchases connected.</p></div><div className="auth-panel"><span className="eyebrow">JOIN LUMA</span><h2>Create account</h2><p>Your account is created on this device with your name and email.</p><button className="primary-btn full" onClick={go}>Create account <ArrowRight size={16}/></button><button className="secondary-btn full" onClick={()=>window.location.href='/login'}>I already have an account</button><small className="auth-legal">Never enter payment PINs or OTPs into LUMA.</small></div></section></main>}